<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title><%= content_for?(:title) ? yield(:title) : "SDREP - Shop 2T" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
  <header>
    <%= render "layouts/nav" %>
  </header>

  <main>
    <%= yield %>
  </main>

  <footer>
    <%= render "layouts/footer" %>
  </footer>
</body>
</html>
